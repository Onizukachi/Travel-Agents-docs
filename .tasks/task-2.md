# Task 2 — Admin Refund Funnel for Tour Operator Statements

## 1) Бизнес контекст для задачи
Нужно сделать админский отчет по воронке возвратов Туроператору. Отчет должен помогать менеджерам быстро находить заказы, по которым у Туроператора есть сумма к возврату, но еще не подано заявление на возврат.

Основная рабочая задача пользователя админки:
- открыть список заказов с ненулевой `operator_payments.amount_awaiting_refund`;
- по умолчанию увидеть только записи без заявления на возврат (`have_written_statement = false`);
- отсортировать список по дате лога, когда менеджер проставил сумму к возврату у ТО;
- понять, кто проставил сумму к возврату и кто проставил заявление;
- перейти из строки в менеджерку заказа.

Итоговые поля таблицы:
- Заказ;
- Заявка;
- ТО;
- Организация;
- У ТО Сумма к возврату;
- Дата лога на возврат;
- Проставил(а) сумму к возврату;
- Заявление;
- Дата лога на заявление;
- Проставил(а) заявление на возврат.

## 2) Ключевые решения и обоснования
1. Реализовать отчет как read-only ActiveAdmin page, а не как новый CRUD-ресурс.
   Обоснование: данные собираются из уже существующих таблиц (`orders`, `operator_payments`, `operators`, `organizations`, `order_logs`) и не требуют отдельной persisted-сущности.

2. Вынести сбор данных в query object в `app/query`, например `Admin::OperatorRefundStatementsQuery`.
   Обоснование: отчетная выборка содержит несколько join-ов, фильтры и вычисляемые поля; controller/page ActiveAdmin должен только принимать параметры и рендерить результат.

3. Базовый набор записей брать из `operator_payments`:
   - `amount_awaiting_refund != 0`;
   - join к `orders`, `operators`, `organizations`;
   - по умолчанию фильтр `have_written_statement = false`.
   Обоснование: это доменная точка, где уже хранятся сумма к возврату, название заявки, ТО, `written_statement_at` и флаг заявления.

4. Дату лога на возврат и менеджера брать из `order_logs` по заказу, `kind = 'action_track'`, сообщению вида `У ТО сумма к возврату добавлено <> руб общая сумма <> руб.`.
   Обоснование: пользователь явно указал этот лог как источник сортировки и автора действия.

5. Менеджера заявления брать из `order_logs` по заказу, `kind = 'action_track'`, сообщению вида `Написано заявление на возврат 24.04.2026`.
   Обоснование: это явный бизнес-сигнал проставления заявления, отдельный от старого аналитического запроса.

6. Сначала реализовать минимальные обязательные фильтры:
   - заявление: `без заявления` / `с заявлением` / `все`;
   - период даты лога на возврат, если в текущих ActiveAdmin отчетах есть устоявшийся паттерн диапазонов;
   - ТО и организация, если их удобно добавить через существующие ActiveAdmin filter/select паттерны без усложнения.
   Обоснование: обязательным из постановки является фильтр по заявлению; остальные фильтры полезны для операционной работы и частично присутствовали в исходном аналитическом SQL.

7. Для заказов с ТО `Level.Travel` не подтягивать данные из analytics/Aurora на первом шаге без отдельного подтверждения.
   Обоснование: текущая задача описывает админку в основном Rails-приложении; исходный Python-запрос использовал внешнюю аналитическую базу только для уточнения названия ТО, что может потребовать отдельного подключения и согласования источника данных.

8. Не добавлять новые гемы.
   Обоснование: для ActiveAdmin read-only отчета достаточно текущего стека, а добавление зависимостей запрещено без явного согласования.

## 3) Структурированный список задач
- [ ] Шаг 1: Найти существующие ActiveAdmin read-only страницы/отчеты и query object паттерны в проекте.
- [ ] Шаг 2: Изучить модели и поля `OperatorPayment`, `Order`, `Operator`, `Organization`, `OrderLog`, а также текущие ссылки из админки на менеджерку заказа.
- [ ] Шаг 3: Уточнить точные route/menu naming для новой страницы в ActiveAdmin и место в меню.
- [ ] Шаг 4: Реализовать query object для строк отчета: базовый scope по `operator_payments`, join-ы к нужным таблицам, выбор полей и расчет статуса заявления.
- [ ] Шаг 5: Добавить выбор лога на возврат: найти подходящий `order_logs` по `order_id`, `kind = 'action_track'`, message pattern `У ТО сумма к возврату добавлено % руб общая сумма % руб.%`; вернуть дату лога и менеджера.
- [ ] Шаг 6: Добавить выбор лога заявления: найти подходящий `order_logs` по `order_id`, `kind = 'action_track'`, message pattern `Написано заявление на возврат %`; вернуть менеджера.
- [ ] Шаг 7: Настроить сортировку по умолчанию по дате лога на возврат.
- [ ] Шаг 8: Реализовать фильтр заявления: по умолчанию `без заявления`, варианты `с заявлением`, `без заявления`, `все`.
- [ ] Шаг 9: Добавить дополнительные фильтры, подтвержденные для первого релиза: ТО, организация, период даты лога на возврат и/или период создания заказа.
- [ ] Шаг 10: Создать ActiveAdmin page с таблицей и ссылкой заказа на менеджерку.
- [ ] Шаг 11: Проверить, что таблица не делает N+1 запросы и не грузит лишние записи в Ruby при обычной пагинации.
- [ ] Шаг 12: Добавить focused RSpec-тесты на query object: базовая выборка, default-фильтр без заявления, фильтр заявления, авторы из логов, сортировка по логу возврата.
- [ ] Шаг 13: Добавить/обновить тесты для ActiveAdmin страницы, если в проекте есть аналогичный паттерн тестирования admin pages.
- [ ] Шаг 14: Запустить targeted specs для измененных файлов.
- [ ] Шаг 15: При необходимости проверить страницу вручную через UI flow ActiveAdmin.

## 4) Заметки для восстановления сессии
### Текущее состояние проекта и уже выполненная работа
- Код реализации еще не изменялся.
- Создан план задачи в `.tasks/task-2.md`.
- Из постановки известно, что нужен ActiveAdmin отчет по заказам с суммой к возврату у ТО и полями из `orders`, `operator_payments`, `operators`, `organizations`, `order_logs`.
- Исходный аналитический SQL содержит лишние для новой админки части: email-лог Библио Глобуса, внешний запрос в analytics/Aurora для переименования `Level.Travel`, фильтры `Библио глобус` и Python-постобработку.

### Критически важные принятые решения
- Отчет проектируется как read-only ActiveAdmin page, а не новая таблица.
- Бизнес-логика выборки должна жить в query object в `app/query`.
- Источник суммы и флага заявления: `operator_payments.amount_awaiting_refund`, `operator_payments.have_written_statement`, `operator_payments.written_statement_at`.
- Источник даты/автора суммы к возврату: `order_logs` с `kind = 'action_track'` и сообщением `У ТО сумма к возврату добавлено ... руб общая сумма ... руб.`.
- Источник автора заявления: `order_logs` с `kind = 'action_track'` и сообщением `Написано заявление на возврат ...`.
- Новые зависимости не добавлять.
- Внешнюю analytics/Aurora логику для уточнения `Level.Travel` не переносить без отдельного подтверждения.

### Точка продолжения при обрыве сессии
1. Получить ответы на открытые вопросы ниже.
2. После ответов начать с поиска существующих ActiveAdmin отчетов и query object паттернов.
3. Реализовать query object и покрыть его focused specs.
4. Добавить ActiveAdmin page, фильтры, пагинацию и ссылку на менеджерку заказа.
5. Запустить targeted specs и при необходимости ручную проверку через ActiveAdmin UI.

### Открытые вопросы перед стартом реализации
1. Где должна находиться новая страница в меню ActiveAdmin и как ее назвать для админов?
2. Нужны ли в первом релизе фильтры из исходного отчета, кроме фильтра заявления: ТО, организация, период даты лога на возврат, период создания заказа?
3. Для ТО `Level.Travel` нужно ли повторять Python-логику с обращением к analytics/Aurora `dynamics`, чтобы показывать `Level.Travel Мой агент` / `Level.Travel ГорТревел`, или в админке достаточно текущего `operators.name`?
4. Сортировка по умолчанию по дате лога на возврат должна быть от старых к новым или от новых к старым?
5. Если по одному `operator_payment` найдено несколько подходящих логов суммы к возврату или заявления, брать последний по времени или лог, ближайший к `awaiting_refund_at` / `written_statement_at`?
